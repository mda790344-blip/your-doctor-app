// Point this at your backend server.
//
// - Running in a WEB BROWSER (npm run web) with backend on the same laptop:
//   "http://localhost:3000" works fine -- browser and server are on the same machine
// - Testing on Expo Go on a PHYSICAL PHONE with backend on your laptop:
//   use your computer's local network IP instead, e.g. "http://192.168.1.42:3000"
//   (localhost won't work from a phone -- it means the phone itself)
// - Once deployed (e.g. to Render, Railway, Fly.io):
//   use your deployed URL, e.g. "https://your-app.onrender.com"

export const API_BASE_URL = "http://localhost:3000";
